extends layout.pug

block content
    h2 Credentials

    if (!credentials || !credentials.length)
        p No Credentials Found
        p Please fill 
    else
        ul
            each c in credentials
                li
                    p #{c.hash}
                    pre #{JSON.stringify(c.raw, null, 2)}
